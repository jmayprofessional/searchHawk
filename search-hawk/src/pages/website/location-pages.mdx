---
import Layout from '../../layouts/Layout.astro';
title: "Location Pages"
---

<Layout title={frontmatter.title}>

<div class="min-h-screen p-8 flex flex-col items-center space-y-8">

  {/* Header */}
  <div class="text-center max-w-3xl">
    <h1 class="text-5xl font-extrabold bg-clip-text text-transparent bg-gradient-to-r from-purple-400 via-pink-500 to-red-500 mb-4">
      Location Pages
    </h1>
    <p class="text-gray-300 text-lg">
      If you serve multiple cities or areas, create a page for each location to match your GBP service areas.
    </p>
  </div>

  {/* Checklist */}
  <div class="w-full max-w-4xl bg-gray-800 rounded-xl shadow-lg p-6 text-gray-100 space-y-4">
    <ul class="list-disc list-inside space-y-2">
      <li>One page per city/area served</li>
      <li>Include consistent NAP for that location</li>
      <li>Service mentions match GBP offerings</li>
      <li>Avoid duplicate content — each location page should be unique</li>
      <li>Internal linking to relevant service pages</li>
    </ul>

    <p class="mt-4 text-gray-400 italic">
      *(Optional: list all target cities and map to pages)*
    </p>
  </div>

</div>

</Layout>
